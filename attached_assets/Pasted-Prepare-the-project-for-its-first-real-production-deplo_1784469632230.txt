Prepare the project for its first real production deployment. Preserve the existing application architecture and functionality. Do not modify business logic except where explicitly requested. Completely remove every remaining development artifact from the project. The current development environment contains a temporary authenticated administrator session using the username dev_user for testing purposes. Ensure that no development authentication sessions, cookies, cached logins, seeded users, temporary administrator sessions, development authentication bypasses, or other testing artifacts remain after deployment. The deployed application must start with no authenticated users. The real dev_user account must be claimable normally through the application's existing registration process after deployment. Review the project for any remaining development configuration, mock data, debugging code, development-only endpoints, console debugging, sandbox payment configuration, placeholder secrets, or temporary testing utilities and remove them from the production build. Create a complete Docker deployment for the entire application. Containerize every required service including the frontend, backend, database, and any additional required services while preserving uploaded user files across container updates. Provide all required Dockerfiles, Docker Compose configuration, startup scripts if necessary, and every supporting configuration required for deployment on a Linux VPS. Do not assume anything is already installed except a clean Linux VPS. After preparing the Docker deployment, write a complete deployment guide intended for someone with absolutely no prior deployment experience. Assume the reader has never used Docker, Linux, Docker Compose, environment variables, reverse proxies, or VPS hosting. Do not skip any step regardless of how basic it may seem. Explain every command before using it. State exactly where every command should be executed. List every required package that must be installed on the VPS. Explain how Docker and Docker Compose are installed. Explain how the project files should be transferred to the VPS. Explain how to build the containers. Explain how to start the application. Explain how to stop it safely. Explain how to restart it. Explain how to update it after future releases while preserving uploaded files and the database. Document every required environment variable individually. For every environment variable, explain what it does, why it is required, where it should be defined, provide an example value, and clearly indicate which values must be replaced with production secrets. Explain how to configure the production database. Explain how uploaded advertisement images and any other persistent files remain available after container recreation. Explain how HTTPS should be configured using a reverse proxy if one is recommended. Explain how the application's domain should point to the VPS. Explain how to verify that the deployment completed successfully. Explain how to inspect application logs. Explain how to troubleshoot common deployment failures. Explain how to create manual database backups. Explain how to restore database backups. Create a production deployment checklist that can be followed before making the application publicly accessible. The checklist should include verification that production secrets are configured, HTTPS is working, sandbox payment mode is disabled, administrator registration works correctly, uploads function correctly, the database persists across restarts, Docker containers restart automatically after server reboot, and backups have been successfully tested. The final deployment guide should be written as a complete beginner's manual with no assumed knowledge and should be sufficiently detailed that a first-time VPS owner can successfully deploy and maintain the application without consulting external tutorials.